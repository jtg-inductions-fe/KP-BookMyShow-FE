/**
 * Represents the authentication state of the application.
 *
 * @property {isAuthenticated} - Indicates whether the user is currently authenticated.
 */
export interface AuthState {
    isAuthenticated: boolean;
}
