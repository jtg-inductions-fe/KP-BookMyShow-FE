import { AvatarProps } from '@mui/material';

/**
 * interface for custom AvatarProps
 **/
export interface StyledAvatarProps extends AvatarProps {
    /**
     * custom size for avatar
     **/
    size?: number;
}
