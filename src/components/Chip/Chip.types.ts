import { ChipProps } from '@mui/material';

/**
 * Interface defining props for `StyledChip` component extending `MUI chip props`.
 */
export interface StyledChipProps extends ChipProps {
    label: string;
}
