import { CircularProgress } from '@mui/material';

import { LoaderContainer } from './Loader.styles';

export const Loader = () => (
    <LoaderContainer>
        <CircularProgress />
    </LoaderContainer>
);
