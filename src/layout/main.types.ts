import { JSX } from '@emotion/react/jsx-runtime';

/**
 * `MainLayoutProps` interface
 *
 * Defines the structure of the props given to the MainLayout.
 */
export interface MainLayoutProps {
    header?: JSX.Element;
}
