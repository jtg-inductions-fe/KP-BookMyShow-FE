export const FilterOptions = ['cancelled', 'past', 'upcoming'];
export const DefaultFilter = 'upcoming';

/**
 * A FilterData for the Filter options for booking status.
 */
export const FilterData = [
    {
        label: 'status',
        filter: 'booking',
        data: FilterOptions,
    },
];
